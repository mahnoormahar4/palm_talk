import 'package:flutter/material.dart';

class TextToGestureScreen extends StatelessWidget {
  const TextToGestureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Text/Voice → Gesture Screen")),
    );
  }
}